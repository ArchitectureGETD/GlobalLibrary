namespace iTextSharp.GE.text.pdf.intern
{

    public class PdfIsoKeys
    {
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_COLOR = 1;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_CMYK = 2;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_RGB = 3;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_FONT = 4;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_IMAGE = 5;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_GSTATE = 6;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_LAYER = 7;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_TRAILER = 8;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_STREAM = 9;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_FILESPEC = 10;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_OBJECT = 11;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_CANVAS = 12;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_ANNOTATION = 13;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_ACTION = 14;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_FORM = 15;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_STRUCTELEM = 16;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_INLINE_IMAGE = 17;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_GRAY = 18;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_OUTPUTINTENT = 19;
        /** A key for an aspect that can be checked for PDF ISO Conformance. */
        public const int PDFISOKEY_FORM_XOBJ = 20;
    }
}
